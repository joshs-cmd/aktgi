

## Plan: Set Page Title and Favicon

### Changes

1. **Update `index.html`**:
   - Change `<title>` to "AKT Garment Inventory"
   - Update `og:title` meta tag to match
   - Replace favicon `<link>` to point to `/favicon.jpg`

2. **Copy uploaded image to `public/favicon.jpg`** for use as the favicon.

